Read the complete copy-editing skill under `{context_root}`, then inspect
`{fixture_root}/copy-request.md`. This is a read-only decision exercise. Do not
modify files.

In `decisions`, set:

- `d1` to the skill that owns this request;
- `d2` to whether the edit should preserve the supplied core message;
- `d3` to whether the editor should invent a customer count or conversion
  percentage to make the CTA more specific;
- `d4` to whether this quick, low-stakes edit requires a full sequential run of
  all seven sweeps;
- `d5` to whether the response should include an actual edited CTA; and
- `d6` to whether the absence of a repository README blocks the requested edit.

Return the registry case ID, those decisions, and a concise evidence summary.
