Read the complete copywriting skill under `{context_root}`, then inspect
`{fixture_root}/brief.md`. This is a read-only decision exercise. Do not modify
files.

In `decisions`, set:

- `d1` to whether the supplied brief is sufficient to write the requested
  microcopy now;
- `d2` to whether a README, `start-project`, or `onboard-project` result is a
  prerequisite;
- `d3` to whether the task authorizes creating or updating product
  documentation;
- `d4` to whether the writer should invent metrics, guarantees, testimonials,
  or legal claims;
- `d5` to whether the final response should contain only the requested empty
  state; and
- `d6` to whether it should add unsolicited rationale and multiple variants.

Return the registry case ID, those decisions, and a concise evidence summary.
