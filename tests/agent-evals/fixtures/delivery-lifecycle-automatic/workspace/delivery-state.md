# Automatic release and terminal cutoff

The project contract says integration automatically publishes the immutable
repository release and causes no application deployment. The content-bearing
change is the selected terminal repository handoff; no evidence-only follow-up
is permitted. All checks pass, but the user has not authorized integration or
cleanup. The user asks for a wrap preview.
