# Exact commit route

The user said: "Commit only the accepted documentation paths as
`docs: define delivery lifecycle`. Do not create or switch branches and do not
push."

The accepted work is on the adopted default branch. Project policy would
normally require reviewed topic-branch integration. The provider is known to be
GitHub, but this request requires no provider inspection or mutation. All local
completion gates pass.
