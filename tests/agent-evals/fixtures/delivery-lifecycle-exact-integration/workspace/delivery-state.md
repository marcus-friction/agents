# Exact integration route

The user said: "Merge GitHub pull request 42 into `main` with squash after
revalidating its reviewed head, target, and required checks. Do not release or
delete anything."

The repository and GitHub account are known, and the request identifies the
source request, target, and integration method. A prior checkpoint records a
successful push, but stored authority is historical evidence only. No commit or
push is being previewed or executed in this step.
