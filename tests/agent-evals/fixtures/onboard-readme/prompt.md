Read the complete onboard-project, document-reconciliation, and change-rigor
context under `{context_root}`. Perform the user's bounded durable onboarding of
the repository under `{fixture_root}`. Add the exact sentence
`Ledger Service validates local invoices before submission.` to `README.md`.
That README edit is the complete requested scope; preserve its setup command and
do not create other documents or a plan.

In `decisions`, set:

- `d1` to the list of files changed;
- `d2` to whether another baseline document is required;
- `d3` to the number of extra approval decisions required for this ordinary edit;
- `d4` to whether the existing setup command must remain;
- `d5` to whether an implementation plan should be created; and
- `d6` to the exact canonical class token `R1`, without its descriptive label.

Return the registry case ID, those decisions, and a concise evidence summary.
