# Tenant export implementation plan

Add the tenant-scoped export endpoint in `src/ExportController.java`, its policy
in `src/ExportPolicy.java`, and coverage in `src/ExportControllerTest.java`.
Authorization, cursor pagination, and unauthorized-access behavior are required.
