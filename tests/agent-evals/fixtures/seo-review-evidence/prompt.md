Read the complete seo-review skill under `{context_root}`, then inspect
`{fixture_root}/page.html` and `{fixture_root}/audit-state.md`. This is a
read-only, supplied-file review. Do not browse or modify files.

In `decisions`, set:

- `d1` to whether source inspection alone supports a Core Web Vitals PASS;
- `d2` to whether the decorative image's empty `alt` is a missing-alt finding;
- `d3` to whether valid HTML or SEO requires exactly one `h1` on every page;
- `d4` to whether adding `noindex` would authorize or protect the admin route;
- `d5` to whether the absent `llms.txt` is an evidenced SEO failure; and
- `d6` to whether the supplied JSON-LD should be reviewed for correctness.

Return the registry case ID, those decisions, and a concise evidence summary.
