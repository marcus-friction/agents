# Change request

An existing skill has a one-character spelling mistake in its YAML description.
The intended trigger boundary, workflow, support files, authority, and outputs
are unchanged. The user asked only to correct the typo and validate the result;
they did not ask to package, install, or publish the skill.
