Read the complete start-project and onboard-project context under
`{context_root}`. Inspect the genuinely new repository fixture under
`{fixture_root}`. Deployment is explicitly unresolved. The first increment uses
only the web interface, and the owner has not overridden the default core stack.
Record a short analysis in `case-note.md`; that is the only authorized fixture
write.

In `decisions`, set:

- `d1` to the complete active baseline documents needed before implementation;
- `d2` to whether the deployment decision has been resolved;
- `d3` to whether local MVP planning can continue with deployment unresolved;
- `d4` to whether the workflow applies a separate conditional profile;
- `d5` to whether unused API and data core components remain adopted but
  deferred; and
- `d6` to whether the owner must separately accept the default core stack.

Return the registry case ID, those decisions, and a concise evidence summary.
