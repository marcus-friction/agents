Read the complete start-project and onboard-project context under
`{context_root}`. Inspect the genuinely new repository fixture under
`{fixture_root}`. Deployment is explicitly unresolved, while the user has chosen
the local MVP components. Record a short analysis in `case-note.md`; that is the
only authorized fixture write.

In `decisions`, set:

- `d1` to the template-backed baseline documents in the explicit candidate
  mapping that must be reconciled before implementation planning; existing
  contextual documents outside that mapping are not part of this set;
- `d2` to whether the deployment decision has been resolved;
- `d3` to whether local MVP planning can continue with deployment unresolved;
  and
- `d4` to whether the workflow applies a separate conditional profile.

Return the registry case ID, those decisions, and a concise evidence summary.
