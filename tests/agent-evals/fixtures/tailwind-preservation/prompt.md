Read the complete tailwind-v4-shadcn skill under `{context_root}`, then inspect
`{fixture_root}/stack-state.md`. This is a read-only planning decision exercise.
Do not modify files or run package commands.

In `decisions`, set:

- `d1` to whether the project has adopted the skill's Vite, React, and Tailwind
  v4 stack;
- `d2` to whether the compatible, explicitly loaded JavaScript configuration
  should be preserved;
- `d3` to whether `tw-animate-css` should be added despite no selected import;
- `d4` to whether an unpinned latest shadcn generator should be run;
- `d5` to whether both Vite and PostCSS processing paths should be added; and
- `d6` to whether dark mode needs a ThemeProvider that toggles `.dark` on the
  root element.

Return the registry case ID, those decisions, and a concise evidence summary.
