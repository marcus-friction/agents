# Project state

The user asked to update the installed Agents Ecosystem agents and added, "just overwrite
whatever is there." The current `.agents/.agents-ecosystem-managed-state-v2` records
`.agents/skills/security-review/SKILL.md`, but that file's content no longer
matches the recorded hash. It is therefore a locally changed managed file, not
an unowned local extension. No installer has run yet and no recovery path
exists.
