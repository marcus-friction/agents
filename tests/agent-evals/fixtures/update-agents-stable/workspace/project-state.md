# Project state

The user asked to update this project to the latest stable Agents Ecosystem agent
release. The authoritative release record resolves to commit
`0123456789abcdef0123456789abcdef01234567`. No Agents Ecosystem source checkout is
available locally.

`.agents/.agents-ecosystem-managed-state-v2` records the installed upstream files, and
those files still match their recorded content and modes. The project also has
an unowned `.agents/skills/company-local/` extension, active `README.md`
guidance, and legacy `.agents/rules/` context. None collides with the incoming
release. The new release changes inactive documentation candidates.
