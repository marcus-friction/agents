Read the complete change-rigor, onboarding, and review context under
`{context_root}`, then inspect `{fixture_root}/change-request.md`. This is a
report-only request. Do not modify files.

In `decisions`, set:

- `d1` to whether R0 permits repository writes;
- `d2` to whether an approved bounded R2 code implementation requires another
  pre-edit patch approval;
- `d3` to whether an unrelated image change restarts approved document work;
  and
- `d4` to whether every R2 change requires adversarial review.

Return the registry case ID, those decisions, and a concise evidence summary.
