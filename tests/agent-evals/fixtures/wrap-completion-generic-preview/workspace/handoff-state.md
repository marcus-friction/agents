# Restore generic wrap commit previews

The user said: "Wrap this up."

The workspace, knowledge, documentation, and quality gates are all resolved.
The accepted bug fix restores the generic-wrap commit preview and consists only
of these two coherent changes:

- `src/wrap-policy.md`
- `tests/wrap-policy-test.md`

They belong in one atomic commit. There is no unrelated or retained work. The
user did not authorize a commit or push, did not exclude either effect, and did
not supply a push destination.
